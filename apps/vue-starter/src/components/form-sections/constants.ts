export const OPTIONS = ['Apple', 'Orange', 'Banana', 'Watermelon'];
export const OBJECT_OPTIONS = OPTIONS.map((item) => ({ value: item, label: item }));
export const STRING_OPTIONS = OPTIONS.toString(); // ''[] toString
